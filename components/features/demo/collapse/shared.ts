/**
 * Shared data for the Collapse write-up: Shot describes both the hero (kept on
 * the page) and the gallery screenshots.
 */
export type Shot = { src: string; w: number; h: number; label: string }
