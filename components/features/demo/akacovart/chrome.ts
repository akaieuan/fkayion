/**
 * Shared shape for the akaCOVART screenshots: the hero (kept on the page) and
 * the gallery strip both describe their images with it.
 */
export type Shot = { src: string; w: number; h: number; label: string }
