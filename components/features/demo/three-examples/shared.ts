/**
 * Shared class strings for the Three.js Examples write-up.
 *
 * The page names a lot of APIs, so `code` earns its place, and it links out to
 * the two neighbouring projects more than once.
 */
export const code = 'rounded bg-muted/50 px-1 py-0.5 text-[13px] text-foreground/85'

export const inlineLink =
  'underline decoration-border underline-offset-[3px] transition-colors hover:text-foreground hover:decoration-foreground/50'
