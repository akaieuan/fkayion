/**
 * Shared class strings for the Three.js Examples write-up.
 *
 * The page names a lot of APIs, so `code` earns its place.
 */
export const code = 'rounded bg-muted/50 px-1 py-0.5 text-13 text-foreground/85'
