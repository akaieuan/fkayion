/** Skill set, end to end. Moved verbatim from app/demo/box-populi/page.tsx. */
export function SkillSetSection() {
  return (
          <section className="space-y-3">
            <h2 className="aka-lead">Skill set, end to end</h2>
            <p className="text-14 leading-relaxed">
              Brand-faithful visual design, typed data modeling, Next.js App Router and React Server
              Components, Tailwind CSS v4, third-party API integration (SoundCloud Widget, Resend),
              cross-origin audio coordination, capability-query responsive design, SEO plumbing,
              headless content modelling for a non-technical editor, Studio theming and
              customisation, migration scripting, webhook-driven cache invalidation, and client
              enablement (editing guide, handoff, spec). Plus the judgment calls, documented in the
              commit history, about which workarounds to ship and which to reject.
            </p>
          </section>
  )
}
