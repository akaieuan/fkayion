'use client'

// This component is intentionally left empty to prevent performance issues.
// The animation logic has been simplified and moved to AudioContext.
export function AnimationSequence() {
  return null
} 