# The BodyLog v1 prototype: a self-contained bundle we host at /demo/bodylog/v1.
# It is a build artifact, not source: 99.8% of it is <script>. The inlined web
# fonts have been stripped out, but it is still the largest single file here and
# without this GitHub's language bar calls the repo mostly-HTML.
public/bodylog-v1.html linguist-vendored

# Transcribed from the trickle registry rather than hand-written. See the note
# at the top of the file.
app/trickle.css linguist-generated
